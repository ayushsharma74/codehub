import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
        <h1>Profile Settings</h1>
        <input type="text" />
        <input type="text" />
        <button>Save</button>
    </div>
  )
}

export default page